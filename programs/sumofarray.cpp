#include<iostream>
using namespace std;
int main() 
{
    int a[10],sum=0;
    cout<<"Enter the number : ";
    for (int i = 0; i < 10; i++)
    {
        cin>>a[i];
    }
    for (int i = 0; i < a[i]; i++)
    {
        sum +=a[i];
    }
    cout<<sum;
    return 0;
}
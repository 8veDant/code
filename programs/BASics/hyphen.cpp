#include<iostream>
#include<string.h>
using namespace std;
int main() 
{
 int i=0;
 char str[80];
 cout<<"Enter the string:";
 gets(str);
 while(str[i]!=0)
 {
        if (str[i]==' ')
     {
        str[i]=='-';
     }
     cout<<"\nThe statement is:"<<str;
 }
return 0;
}